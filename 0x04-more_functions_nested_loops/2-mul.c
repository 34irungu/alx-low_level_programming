#include "main.h"

/**
 * mul - computes the multiplication of two integers
 *
 * @a: first parameter
 * @b: second parameter
 * Return: The result of the multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

#include "main.h"
#include <stdio.h>
/**
 * add - computes the addition of two integers
 *
 * @k: first integer
 * @l: second integer
 * Return: The result of the addition.
 */
int add(int k, int l)
{
	int sum = k + l;

	return (sum);
}
